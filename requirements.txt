# REST analysis uses Python standard library only.
# Realtime futures prices prefer direct Binance websocket support.
websockets>=12.0

# Optional fallback websocket client used when available.
python-binance>=1.0.19

# Optional persistent storage. Install when BIAN_MYSQL_* or BIAN_REDIS_* is used.
pymysql>=1.1.0
redis>=5.0.0
